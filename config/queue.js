// const { Queue } = require('bullmq');
// const Redis = require('ioredis');

// const redisConnection = new Redis({
//   host: '127.0.0.1',
//   port: 6379,
//   // Add password or TLS config if needed
// });

// const campaignQueue = new Queue('campaignQueue', { connection: redisConnection });

// module.exports = { campaignQueue, redisConnection };
